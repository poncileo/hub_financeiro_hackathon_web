# 🎯 Login Implementation - System Architecture

## 📐 Arquitetura Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                     FRONTEND (React + Vite)                      │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  React Components                       │   │
│  │                                                         │   │
│  │  ┌──────────────┐      ┌──────────────┐              │   │
│  │  │  Login.jsx   │      │ Signup.jsx   │              │   │
│  │  └──────┬───────┘      └──────┬───────┘              │   │
│  │         │                     │                       │   │
│  │         └──────────┬──────────┘                       │   │
│  │                    ↓                                   │   │
│  │         ┌──────────────────────┐                      │   │
│  │         │  AuthContext.jsx     │                      │   │
│  │         │  (useAuth Hook)      │                      │   │
│  │         └──────────┬───────────┘                      │   │
│  │                    ↓                                   │   │
│  │         ┌──────────────────────┐                      │   │
│  │         │ authService.js       │                      │   │
│  │         │ - login()            │                      │   │
│  │         │ - signup()           │                      │   │
│  │         │ - logout()           │                      │   │
│  │         └──────────┬───────────┘                      │   │
│  │                    ↓                                   │   │
│  │         ┌──────────────────────┐                      │   │
│  │         │ api.js (apiCall)     │                      │   │
│  │         │ - Headers            │                      │   │
│  │         │ - Auth Token         │                      │   │
│  │         │ - Error Handling     │                      │   │
│  │         └──────────┬───────────┘                      │   │
│  │                    ↓                                   │   │
│  │         ┌──────────────────────┐                      │   │
│  │         │ localStorage         │                      │   │
│  │         │ - authToken          │                      │   │
│  │         │ - user               │                      │   │
│  │         └──────────────────────┘                      │   │
│  │                                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│                            HTTP/REST                             │
│  ╔═══════════════════════════════════════════════════════════╗  │
│  ║         POST /api/auth/login                              ║  │
│  ║         POST /api/users (signup)                          ║  │
│  ║         POST /api/auth/logout                            ║  │
│  ╚═══════════════════════════════════════════════════════════╝  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Bearer Token
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   BACKEND (Node.js/Express)                      │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            authController.ts                           │   │
│  │                                                         │   │
│  │  POST /login    POST /logout                          │   │
│  │      ↓              ↓                                   │   │
│  │  ┌────────────────────────────────────────┐           │   │
│  │  │      authService.ts                    │           │   │
│  │  │  - Validação de credenciais            │           │   │
│  │  │  - Geração de JWT                      │           │   │
│  │  │  - Verificação de senha (bcrypt)       │           │   │
│  │  └────────────────────────────────────────┘           │   │
│  │                    ↓                                    │   │
│  │  ┌────────────────────────────────────────┐           │   │
│  │  │   userController.ts                    │           │   │
│  │  │   POST / (criar novo usuário)          │           │   │
│  │  └────────────────────────────────────────┘           │   │
│  │                    ↓                                    │   │
│  │  ┌────────────────────────────────────────┐           │   │
│  │  │     userService.ts                     │           │   │
│  │  │  - Criação de usuário                  │           │   │
│  │  │  - Hash de senha                       │           │   │
│  │  │  - Validações de negócio               │           │   │
│  │  └────────────────────────────────────────┘           │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           PostgreSQL Database                          │   │
│  │                                                         │   │
│  │   ┌────────────────────────────────────────────────┐   │   │
│  │   │ users table                                    │   │   │
│  │   │ - id                                           │   │   │
│  │   │ - name                                         │   │   │
│  │   │ - email (UNIQUE)                              │   │   │
│  │   │ - password (hashed)                           │   │   │
│  │   │ - balance                                      │   │   │
│  │   │ - credit_score                                │   │   │
│  │   │ - type (ADMIN, NORMAL)                        │   │   │
│  │   │ - created_at, updated_at                      │   │   │
│  │   └────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo de Autenticação Detalhado

### Login:
```
1. User Clicks "Entrar"
        ↓
2. Frontend Validation
   ├─ Email check (format)
   ├─ Password check (min length)
   └─ Form validation
        ↓
3. HTTP Request
   ├─ Method: POST
   ├─ Endpoint: /api/auth/login
   ├─ Body: { email, password }
   └─ Headers: { Content-Type: application/json }
        ↓
4. Backend Processing
   ├─ Find user by email
   ├─ Compare password (bcrypt)
   ├─ Generate JWT token
   └─ Return { user, token }
        ↓
5. Frontend Response Handling
   ├─ Save token to localStorage
   ├─ Save user data to localStorage
   ├─ Update AuthContext state
   └─ Redirect to /
        ↓
6. Protected Routes Available
   └─ ProtectedRoute checks token
```

### Signup:
```
1. User Fills Form
   ├─ name
   ├─ email
   ├─ password
   ├─ confirmPassword
   └─ acceptTerms
        ↓
2. Frontend Validation
   ├─ Name (min 3 chars)
   ├─ Email (valid format)
   ├─ Password (min 6 chars)
   ├─ Passwords match
   └─ Terms accepted
        ↓
3. Create User Request
   ├─ Method: POST
   ├─ Endpoint: /api/users
   ├─ Body: { name, email, password, type, balance, creditScore, configuration }
   └─ Headers: { Content-Type: application/json }
        ↓
4. Backend Processing
   ├─ Validate input
   ├─ Check if email exists
   ├─ Hash password (bcrypt)
   ├─ Create user in database
   └─ Return new user
        ↓
5. Auto Login
   ├─ Method: POST
   ├─ Endpoint: /api/auth/login
   ├─ Body: { email, password }
   └─ (Same as login flow above)
```

## 📦 Estrutura de Dados

### LocalStorage After Login:
```javascript
{
  authToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiIxIiwiZW1haWwiOiJ1c2VyQGVtYWlsLmNvbSJ9.xyz...",
  user: {
    id: 1,
    name: "João Silva",
    email: "joao@email.com",
    type: "NORMAL",
    balance: 1000.00,
    creditScore: 750,
    pixKeys: [],
    configuration: {}
  }
}
```

### API Request Headers:
```
POST /api/auth/login HTTP/1.1
Host: localhost:3000
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### API Response:
```json
{
  "message": "Login realizado com sucesso",
  "user": {
    "id": 1,
    "name": "João Silva",
    "email": "joao@email.com",
    "type": "NORMAL"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

## 🛡️ Security Layers

```
┌─────────────────────────────────────────┐
│  Frontend Validation                    │
│  (Email format, password length, etc)   │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  HTTPS/TLS Transport                    │
│  (Encrypt data in transit)              │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  Backend Validation                     │
│  (Email exists, format check, etc)      │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  Password Hashing (bcrypt)              │
│  (Compare hashed passwords)             │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  JWT Token Generation & Verification    │
│  (Signed token with secret)             │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  Authorization Middleware               │
│  (Check token on protected routes)      │
└─────────────────────────────────────────┘
```

## 📊 State Management Flow

```
Component (Login.jsx)
    │
    ├─ Local State: email, password, error, loading
    │
    ├─ useAuth() Hook
    │
    └─ AuthContext
       │
       ├─ State: user, loading, error, isAuthenticated
       │
       ├─ login(email, password)
       │  └─ authService.login()
       │     └─ apiCall() POST /auth/login
       │        └─ Save token + user to localStorage
       │           └─ Update AuthContext state
       │              └─ Component re-renders
       │
       └─ useEffect Hook
          └─ Check localStorage on mount
             └─ Restore session if valid
```

## 🚀 Component Lifecycle

```
App.jsx mounts
    ↓
AuthProvider wraps app
    ↓
AuthContext.useEffect
    ├─ Check localStorage for token
    ├─ Restore user session if valid
    └─ Set loading to false
    ↓
Login/Signup component mounts
    ├─ useEffect checks if already authenticated
    ├─ If yes, redirect to /
    └─ If no, show form
    ↓
User interacts with form
    ├─ Validation runs
    └─ API call on submit
    ↓
API Response
    ├─ Success: Save to localStorage → Redirect
    └─ Error: Show error message
```

## 🔗 File Dependencies

```
Login.jsx
├─ useAuth() → AuthContext.jsx
├─ useNavigate() → react-router-dom
├─ MdWarning, MdCheckCircle → react-icons
└─ Login.css

Signup.jsx
├─ useAuth() → AuthContext.jsx
├─ useNavigate() → react-router-dom
├─ MdWarning, MdCheckCircle → react-icons
└─ Signup.css

AuthContext.jsx
├─ authService → authService.js
└─ createContext, useContext, useState, useEffect

authService.js
└─ apiCall → api.js

api.js
└─ import.meta.env (Vite)
```

## 📋 Configuration

```
.env (Development)
├─ VITE_API_URL=http://localhost:3000/api
└─ VITE_ENV=development

Backend Port: 3000
Frontend Port: 5173
Database: PostgreSQL
```

---

**Architecture Version:** 1.0.0  
**Last Updated:** 27 de novembro de 2025  
**Status:** ✅ Documented
